git init => git initialization

git status => list all untracked files

git add -A => adding all files to staging area

git commit -m "message" =>moving staged files to local registery

git clone  => exact copy of remote repository to local workspace

git push    => local workspace to remote repository

git pull    => remote repository to local workspace

git remote add name remote_path 

